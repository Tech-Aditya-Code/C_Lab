#include <stdio.h>

int main()
{
    // char only contain single char.
    // ASCII values
    // syntax '' single quote

    char ch = '*'; // 65
    printf("%c",ch);

    // char ch = 'a';  // output 97
    // printf("%d",ch);

    return 0;
}