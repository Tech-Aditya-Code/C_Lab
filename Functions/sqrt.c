#include <stdio.h>
#include <math.h>

int main()
{
    // int a;
    // printf("Enter a number: ");
    // scanf("%d", &a);
    // int root = sqrt(a);
    // printf("The sqrt is: %d", root);
    int q = sqrt(49);
    printf("%d",q);

    return 0;
}